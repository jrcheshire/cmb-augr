name: augr
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - jax
      - numpy
      - scipy
      - matplotlib
      - pytest
      - camb  # only needed to regenerate data/ templates via generate_camb_templates.py
      - jupyterlab
      - ipykernel
